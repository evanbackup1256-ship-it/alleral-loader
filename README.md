# Alleral

Releases are generated automatically by GitHub Actions whenever commits are pushed to the `main` branch. Each push creates a new release with a timestamp-based tag, commit details, and the list of changed files.

See [Releases](https://github.com/evanbackup1256-ship-it/kick/releases) for the latest builds.
